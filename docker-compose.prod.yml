services:
  nginx:
    image: nginx:1.29-alpine
    container_name: maroquiz-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/docker/nginx/default.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - laravel_shared:/var/www/html:ro
      - frontend_dist:/var/www/frontend:ro
      - certbot_certs:/etc/letsencrypt:ro
      - certbot_webroot:/var/www/certbot:ro
    depends_on:
      php:
        condition: service_started
      frontend:
        condition: service_completed_successfully
    networks:
      - maroquiz-network

  php:
    build:
      context: .
      dockerfile: infrastructure/docker/php/Dockerfile.prod
    container_name: maroquiz-php
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - laravel_shared:/var/www/shared
      - laravel_storage:/var/www/html/storage
    depends_on:
      - postgres
      - redis
    networks:
      - maroquiz-network

  # One-shot build job: compiles the Angular app and copies the static output
  # into a shared volume, then exits. nginx serves that volume directly.
  frontend:
    build:
      context: .
      dockerfile: infrastructure/docker/frontend/Dockerfile.prod
    container_name: maroquiz-frontend-build
    volumes:
      - frontend_dist:/output
    networks:
      - maroquiz-network

  postgres:
    image: postgres:17
    container_name: maroquiz-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    # No "ports:" — only reachable by other containers on maroquiz-network.
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - maroquiz-network

  redis:
    image: redis:8-alpine
    container_name: maroquiz-redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    # No "ports:" — only reachable by other containers on maroquiz-network.
    volumes:
      - redis_data:/data
    networks:
      - maroquiz-network

networks:
  maroquiz-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  laravel_shared:      # public/ + baked-in code, shared between php and nginx
  laravel_storage:      # persists uploads/logs across php container rebuilds
  frontend_dist:
  certbot_certs:
  certbot_webroot: